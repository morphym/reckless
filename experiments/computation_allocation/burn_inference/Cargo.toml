[package]
name = "reckless-cs-burn"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
burn = { version = "0.21.0", default-features = false, features = ["std", "flex"] }
burn-store = "0.21.0"
serde_json = "1.0"
